{
  "name": "clarokpis",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "npx live-server --port=5500 --open=dashboard.html",
    "test": "node tests/smoke.test.js"
  },
  "devDependencies": {
    "terser": "^5.48.0"
  }
}